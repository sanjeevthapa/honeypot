<%= content_for(:title, "User Profile") %>
<p>
<%= link_to "Add Resume", user_add_resume_index_url, class: "button border fw margin-top-10" %>
</p>
<p>
<% flash.each do |key, value| %>
				   <div class="alert alert-success"><%= value %></div>
<% end %>
</p>
<div class="container-fluid">
	<h1 style="color:black;">The following information are stored in database:</h1>
	<ul>
		<li>Id: <%= current_user.id %></li>
		<li>Name: <%= current_user.username.capitalize %></li>
		<li>Email: <%= current_user.email %></li>
		<li>Password digest: <%= current_user.password_digest %></li>
		<li>Created at: <%= current_user.created_at %></li>
		<li>Updated at: <%= current_user.updated_at %></li>
		<li>Auth token: <%= current_user.auth_token %></li>
		<li>Password reset token: <%= current_user.password_reset_token %></li>
		<li>Password reset token sent at: <%= current_user.password_reset_sent_at %></li>
		<li>Verify token: <%= current_user.verify_token %></li>
		<li>Verify token sent at: <%= current_user.verify_token_sent_at %></li>
		<li>Status: <%= current_user.status %></li>
		<li>Terms: <%= current_user.terms %></li>
	</ul>
</div>